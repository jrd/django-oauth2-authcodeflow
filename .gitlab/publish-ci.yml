include:
  - local: '/.gitlab/python-ci.yml'
.publish:
  extends:
    - .python
  stage: publish
  when: manual
  rules:
    - if: $CI_COMMIT_TAG
  before_script:
    - |
  script:
    - |
      set -e
      poetry config pypi-token.pypi "$PYPI_TOKEN"
      make pypi_upload
